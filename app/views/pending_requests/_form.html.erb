<%= form_for(@pending_request) do |f| %>
  <% if @pending_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pending_request.errors.count, "error") %> prohibited this pending_request from being saved:</h2>

      <ul>
      <% @pending_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
