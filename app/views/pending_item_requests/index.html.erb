<p id="notice"><%= notice %></p>

<h1>Listing Pending Item Requests</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pending_item_requests.each do |pending_item_request| %>
      <tr>
        <td><%= link_to 'Show', pending_item_request %></td>
        <td><%= link_to 'Edit', edit_pending_item_request_path(pending_item_request) %></td>
        <td><%= link_to 'Destroy', pending_item_request, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pending item request', new_pending_item_request_path %>
